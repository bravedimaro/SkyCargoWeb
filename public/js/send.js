// Theme language-switcher demo data.
// The app's real UI translations are rendered server-side (public/language/languages.json),
// so this client-side `trans` table is only used by the decorative top-bar language widget.
// It was missing from the package, which caused a 404 and a "trans is not defined" error.
var trans = [];
